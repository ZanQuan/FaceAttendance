<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#F5F5F5">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📊 Xuất báo cáo Excel"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#1A237E"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="File Excel sẽ được lưu vào bộ nhớ máy"
        android:textSize="13sp"
        android:textColor="#757575"
        android:layout_marginBottom="24dp"/>

    <!-- Chọn lớp -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Chọn lớp (để trống = tất cả lớp)"
        android:textSize="13sp"
        android:textColor="#5C6BC0"
        android:layout_marginBottom="6dp"/>

    <Spinner
        android:id="@+id/spinnerClass"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_input_field"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginBottom="24dp"/>

    <!-- Nút xuất danh sách điểm danh -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnExportAttendance"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="📋 Xuất danh sách điểm danh"
        android:textSize="15sp"
        android:backgroundTint="#3949AB"
        android:layout_marginBottom="12dp"/>

    <!-- Nút xuất danh sách đi trễ -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnExportLate"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="⚠️ Xuất danh sách đi trễ"
        android:textSize="15sp"
        android:backgroundTint="#E53935"
        android:layout_marginBottom="24dp"/>

    <!-- Progress -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="#2E7D32"
        android:gravity="center"
        android:visibility="gone"/>

</LinearLayout>